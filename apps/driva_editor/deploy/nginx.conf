server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  gzip on;
  gzip_types text/plain text/css application/javascript application/json application/wasm image/svg+xml;
  gzip_min_length 1024;

  # Entry HTML: precisa revalidar em toda abertura/refresh para descobrir
  # builds novas. O fallback SPA em / tambem herda esta politica.
  location = /index.html {
    add_header Cache-Control "no-cache, max-age=0, must-revalidate" always;
    try_files $uri =404;
  }

  # Service worker legado: a build atual usa --pwa-strategy=none e o bootstrap
  # tambem desregistra SWs antigos. Este arquivo nunca pode ficar preso em cache.
  location = /flutter_service_worker.js {
    add_header Cache-Control "no-store, max-age=0" always;
    try_files $uri =404;
  }

  # Scripts/wasm/json do Flutter nao sao fingerprintados de forma consistente
  # (ex.: main.dart.js, flutter_bootstrap.js), entao devem revalidar sempre.
  location ~* \.(?:js|mjs|wasm|json)$ {
    add_header Cache-Control "no-cache, max-age=0, must-revalidate" always;
    try_files $uri =404;
  }

  # Fontes: precisam revalidar sempre. O Flutter faz tree-shaking dos icones
  # num MaterialIcons-Regular.otf de nome ESTAVEL entre builds — cachear por
  # nome deixava o navegador com um subset antigo, sem os codepoints de icones
  # novos (ex.: o botao de tema virava "tofu"). Com no-cache o browser
  # revalida via ETag: 304 quando igual, bytes novos quando o subset muda.
  location ~* \.(?:eot|otf|ttf|ttc|woff|woff2)$ {
    add_header Cache-Control "no-cache, max-age=0, must-revalidate" always;
    try_files $uri =404;
  }

  # Imagens/CSS: nome tambem pode repetir entre builds, entao cache curto e
  # nao imutavel.
  location ~* \.(?:png|jpg|jpeg|gif|svg|webp|ico|css)$ {
    add_header Cache-Control "public, max-age=3600" always;
    try_files $uri =404;
  }

  # Assets do Flutter: mantem fallback 404 explicito para arquivos inexistentes.
  location /assets/ {
    try_files $uri =404;
  }

  # SPA fallback: rotas do go_router resolvem no cliente.
  location / {
    add_header Cache-Control "no-cache, max-age=0, must-revalidate" always;
    try_files $uri $uri/ /index.html;
  }
}
