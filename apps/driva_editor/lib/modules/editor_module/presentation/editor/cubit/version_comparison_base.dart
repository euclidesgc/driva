/// Qual spec joga o papel de "base" (à esquerda) numa comparação (T5, item
/// 50): o rascunho por padrão, ou a versão no ar quando o usuário troca. A
/// cópia seletiva sempre incide sobre o rascunho de verdade, nunca sobre o
/// que está sendo exibido — ver `VersionCompareCubit.copyNodeProperties`.
enum VersionComparisonBase { draft, published }
