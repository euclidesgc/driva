# Roadmap — driva

Rastreamento vivo do que está **feito**, **em andamento** e **por fazer**. A lista está ordenada **por dependência**: o que vem antes destrava o que vem depois. Cada item traz, entre parênteses, o número original em `docs/03-melhorias/` para rastreabilidade.

**Legenda:** `[ ]` não iniciada · `[-]` em andamento · `[x]` concluída.

> **Documento vivo.** Mantido atualizado pela IA a cada fechamento de trabalho (junto da faxina de branches): marca o item entregue como `[x]`, o item da vez como `[-]`, e — quando surgem features novas — reescreve o texto para dar clareza e **reordena** para o ponto de precedência correto. Ver `CLAUDE.md` › _Método de trabalho_.

> **Todo item aberto tem um plano.** Cada item por fazer aponta para uma pasta em **`docs/plans/<NN>-<slug>/plan.md`** com o planejamento detalhado (fases, arquivos, classes, métodos, precedências e o que dá para paralelizar) — escrito **antes** de codar, para que a entrega possa ser validada no papel. Índice: [`docs/plans/README.md`](plans/README.md). Quando a feature entra em execução, a doc viva do trabalho (`docs/NN-<nome>/`) nasce a partir do plano.

---

## Ordem de execução recomendada

O que destrava o quê, em uma linha. Os números são identidade do item (histórica), **não** posição na fila.

```
✔ 38, 39*, 23, 9 ─► ✔ 24 (publicação) ─┬─► 25 (entrega ao app) ─┬─► 26 (auth) ─► 27 (storage)
                                       │   └ fatia 2 no PR #182 │
                                       │     falta o E2E humano ├─► 28 (eventos/ações) ─► 29 (dados/binding)
                                       │                        │
                                       │                        └─► 19 ─► 20 ─► 21 ─► 22 (componentes)
                                       └─► 50 (histórico seguro)   ← ∥ com o 25, arquivos disjuntos

17 ─► 18 (offline-first)   8b (JSON)   30 (breakpoints)   43, 45, 47, 48, 49   ← encaixáveis, sem dependência
```
`✔` = entregue. `39*` = F2/F3/F4 entregues, **falta só a bateria (F5)**.

**O gargalo era o 24 + 25, e ele está caindo.** O 24 fechou em 2026-08-17; o **código** da fatia 2 do 25 está no PR #182 com CI verde (`packages/driva_client`, o app de demonstração migrado, rate limit da rota pública). O que separa o driva de "entrega conteúdo para app cliente" é agora **um E2E atestado por humano**, não código a escrever.

**O item 50 é a única frente que corre em paralelo com o 25 hoje**: nasce do item 24, vive em `apps/driva_editor` + `backend/src/contents`, e não encosta no `packages/driva_client` que a fatia 2 do 25 cria do zero.

**Os dois defeitos que entraram na frente do 24 (2026-08-15) foram corrigidos.** O **38** (página sem saída depois de um drop recusado) fechou; o **39** (imagem falhando em silêncio, indistinguível de "sem URL") teve as três fases de código entregues — proxy de mídia, resolver e `image` abrangente. **O que ainda segura o 39 é a bateria (F5)**, e antes dela as três asserções que o E2E da rodada 01 não conseguiu provar: que o campo de dimensão distingue "ajustado ao mínimo" de "valor inválido".

---

## Base já entregue

- `[x]` **Fundação I1 — Conteúdos (rename página→conteúdo + identidade slug/CUID2).** No ar em homologação; renderer SDUI, editor de 3 painéis, catálogo com 24 widgets, backend `/v1/contents`. É o alicerce sobre o qual todo o resto abaixo é construído.

---

## Marco 0 — Fundação e correções que destravam tudo

_Sem dependências entre si; vieram primeiro porque tornam todo o resto viável ou agradável de construir._

- `[x]` **1. Corrigir o bug de foco no Inspector (0-dep).** _(item 16)_ Ao digitar em qualquer campo de propriedade (ex.: elevação do card), o editor perdia o foco após cada tecla e exigia reclicar. Causa: a `ValueKey` do `TextFormField` incluía o valor, então cada `onChanged` recriava o campo. **Precede o item 9 (catálogo)** — sem isso, editar propriedades era inviável.
- `[x]` **2. Enxugar loadings e rebuilds da navegação.** _(item 10)_ Create navega direto ao editor (sem `await load()`/spinner) e exclusão otimista (card some na hora, reconcilia em falha). Docs em `docs/05-loadings-navegacao/`.
- `[x]` **3. Tema light + dark com persistência.** _(item 0)_ Introduziu a camada de preferências local (`preferences_module`, `shared_preferences`) que o **item 17 (offline-first)** reaproveita.
- `[x]` **3b. Aliviar o peso do editor ao digitar/arrastar (escopo de rebuilds).** _(perf; surgiu do uso)_ Rebuilds escopados por painel (`BlocSelector`/`buildWhen`), canvas isolado (`RepaintBoundary`) e preview caro throttlado, mantendo campo e estado instantâneos.

## Marco 1 — Polimento do construtor (canvas)

- `[x]` **4. Altura máxima do mock do dispositivo.** _(item 12)_ ~~Limitar a altura do mock.~~ **Revertido:** o teto encolhia o dispositivo em janelas baixas; o **zoom** (80%) já resolve o encaixe.
- `[x]` **5. Feedback visual ao soltar um componente no mock + realce no hover.** _(item 11)_
- `[x]` **6. Molduras de dispositivo realistas.** _(item 13)_
- `[x]` **7. Painel de preview do JSON em tempo real.** _(item 14)_
- `[x]` **8. JSON somente-leitura, copiável e com syntax highlight.** _(item 15)_
- `[x]` **8c. Raiz livre (página nasce vazia; 1º widget vira a raiz).** _(estilo FlutterFlow)_ `ContentSpec.root` opcional; o primeiro widget adicionado — de qualquer tipo — vira a raiz.
- `[x]` **8d. Raiz folha mostra o estado-vazio em vez de renderizar.** _(bug; `09f3bf7`)_ Condição virou só `root == null`. Coberto por teste de regressão em `editor_perf_test.dart`.

## Marco 1b — Manipulação direta no construtor

- `[x]` **8e. Mover widgets no mock + barra de status de problemas.** _(docs/12)_ Todo nó virou origem e destino de arraste; frestas de inserção na árvore; `resolveDrop` (kernel) como regra única dos dois painéis; rodapé com os problemas do documento (`diagnoseTree`).
- `[x]` **8f. Área segura obrigatória em toda página.** _(docs/12)_ `SafeArea` como **chrome da página** (`ContentSpec.safeArea`), fora do catálogo; o mock injeta o recuo real do dispositivo no `MediaQuery`.
- `[ ]` **43. Chrome da página: barra de topo (appBar) e barra do sistema.** _(reescopado em 2026-08-17 — absorve o pedido novo de fidelidade construtor × app; era só "estilo da barra do sistema", pedido do humano em 2026-08-16)_ **As duas metades que faltam do 8f.** O `safeArea` do 8f governa o **recuo**; ele não governa nem a **barra de topo** nem a cor dos ícones do sistema — e hoje nada no driva governa: `AppBar`, `Scaffold`, `SystemUiOverlayStyle` e `AnnotatedRegion` têm **zero ocorrências** no kernel e no renderer. Docs vivas em `docs/43-chrome-da-pagina/` (`specs.md` + `prd.md`, discovery de 2026-08-17).
  - **O pedido novo, de uso real (2026-08-17):** o dev gerou um APK release do `apps/driva_demo_app` contra homologação e abriu um conteúdo publicado num Android físico. O app desenhava **casca própria** (`AppBar` com título/recarregar + rodapé de debug com data do spec e `etag`) que **não vem do spec** — o que ele viu não batia com o que montou no editor. _"precisamos de fidelidade entre o construtor web e o app. talvez a gente esteja precisando de um componente de appbar no catálogo de widgets"_. A casca **continua em `develop`** (`apps/driva_demo_app/lib/modules/published_module/presentation/content/page/content_page.dart:34-47` ainda monta `Scaffold(appBar: AppBar(…), body: ContentBody(), bottomNavigationBar: ContentMetaBar())`; `content_meta_bar.dart` não foi apagado) — a remoção existe só na branch parada `parked/demo-app-internet-e-casca`. **Decisão do humano em 2026-08-17 (A14):** tirar a casca **da tela de conteúdo** — só o `appBar:` e o `bottomNavigationBar:`, mantendo o `Scaffold` externo, que serve os estados de carregamento e erro — **entra neste item, como F0**; o resto da casca segue com o item 25. Sem a F0, o E2E em aparelho deste item mediria barra dupla, não fidelidade. O episódio **expôs a lacuna real** — o spec não sabe expressar uma barra de topo — e o discovery (`docs/43-chrome-da-pagina/specs.md`) foi escrito sobre a premissa contrária, de que a casca já tinha saído: é o que a **A14** resolveu.
  - **O sintoma antigo continua valendo:** uma página de fundo escuro deixa a faixa da barra escura, e num aparelho com ícones escuros **o relógio e a bateria somem** — falha que só aparece em homologação, porque o editor não tem como mostrá-la.
  - **Por que virou um item só (recomendação do PM, a confirmar pelo humano — A7):** mesmo `ContentSpec`, mesmo bloco do Inspector sem seleção, mesmo `SduiView`, mesmo desenho no mock e **o mesmo E2E em aparelho físico** (a parte cara — só o dev humano tem o aparelho). E há acoplamento duro, verificado no Flutter 3.44.9 (a versão do projeto): a `AppBar` já se embrulha num `AnnotatedRegion<SystemUiOverlayStyle>` derivado do **próprio `backgroundColor`** (`app_bar.dart:1229`) — ou seja, **havendo appBar é a cor dela, não a da página, que decide se o relógio some.** Fazer o 43 antes da appBar modelaria o contraste contra a superfície errada.
  - **O que entra:** a barra de topo e o estilo do sistema viram **dado do spec**, ao lado do `safeArea` (mesma natureza de chrome, fora do `widgetCatalog`, editados pelo Inspector quando nenhum nó está selecionado) — o editor não executa, quem aplica é o app cliente. Toca **três camadas**: `sdui_core` (descriptor + schema), `sdui_flutter` (`Scaffold`/`AppBar`/`AnnotatedRegion` no `SduiView`) e o editor (Inspector + mock). **Backend: zero trabalho** (JSONB passa reto). Recorte proposto: **5 fases** (kernel · renderer · Inspector · barra do sistema · E2E+bateria).
  - **O custo real não é a `AppBar` — é a rolagem.** Dois dos três consumidores do `SduiView.content` embrulham o renderer num `SingleChildScrollView` (`preview_surface.dart:103`, `preview_content.dart:40`), o que entrega altura infinita: qualquer `Scaffold`/`Expanded` estoura. A barra obriga o scroll a **migrar para dentro** do `SduiView` nos três. Herdam de graça: o app de demonstração e o **device mock** (o `device_status_bar.dart` já foi escrito para não pintar fundo, justamente por causa deste item). Tem trabalho próprio: o `/preview` (banner e pílula são `Positioned(top:)` e ficariam atrás da barra).
  - **Decisão do humano (2026-08-16, não reabrir):** iOS e Android desenham a barra de forma diferente, e **para o objetivo (prever contraste) isso não é relevante** — um desenho só serve os dois.
  - ⚠️ **Onze decisões pendentes do dev, com recomendação do PM em `docs/43-chrome-da-pagina/specs.md` › _Decisões do humano_. Nenhuma fase começa antes.** As três que mais mudam o produto: **A2** quem monta o `Scaffold` (rec.: o `SduiView.content` vira dono da tela, com `bodyWrapper` para os hosts) · **A3** props do v1 (rec.: `enabled`, `title`, `backgroundColor`, `foregroundColor`, `centerTitle` — `elevation` e `showBackButton` cortados por baixo valor e risco semântico; `actions` **fora**, depende do item 28) · **A7** fundir com o 43 (rec.: sim, um item em fases, um E2E só). A pendência antiga de formato da prop foi respondida pela **A9**: campos planos irmãos do `safeArea`, sem migração. E a retrocompatibilidade tem um jeito certo: `enabled` com `defaultValue: false` — clonar o `safeArea` literalmente daria **barra em todo conteúdo já publicado**.
  - **Precede a utilidade da 5.1 (F3 do item 41).** A F3 desenha a status bar no mock, e a **D29** manda desenhá-la com cores de token próprias — o que a torna **sempre legível no mock, inclusive quando no aparelho ela sumiria**. Um mock assim não prevê o problema: mascara. A F3 recebeu a nota de desenhar a barra **transparente sobre o fundo real da página**; a cor dos ícones passa a ter fonte quando este item entrar.
  - ⚠️ **O discovery de código foi feito pelo PM, não pelo tech-lead** (o agente acionado não retornou na rodada de 2026-08-17). Caminhos e linhas citados no `specs.md` foram conferidos no repo, mas **o recorte de 5 fases é proposta, não plano validado** — o tech-lead precisa revisar o §3 (os três consumidores do `SduiView.content`) e a A2 antes de escrever o `plan.md`.
  - **Precedência sugerida (PM):** destravar antes o item 46 (discovery fechado, esperando só o `plan.md`, 1 PR). ⚠️ E conferir o item 41 F8/F9 antes de abrir a fase do renderer — mexer no `preview_content.dart` com o E2E daquele item ainda não atestado move o alvo debaixo do QA.
- `[ ]` **48. Tema e design tokens do projeto, em cascata.** _(pedido do humano, 2026-08-17 — ideia que ele já vinha amadurecendo; **anotado para aprofundar depois**, sem discovery ainda)_ **A terceira metade da mesma pergunta do 8f e do 43: o que o spec não diz, quem decide?** Hoje a resposta é "quem hospeda o renderer", e isso é um defeito visível: um `button` **sem cor definida** sai **laranja no editor** (herda o tema do Driva Builder) e **lilás no app cliente** (herda o `ColorScheme.fromSeed` do app) — mesmo spec, duas cores. Causa mecânica: `buildButton` passa `backgroundColor: null` ao `ElevatedButton.styleFrom` (`packages/sdui_flutter/lib/src/builders/button.dart:15`), e cor nula significa "usa o tema do ambiente"; nem o mock nem a rota `/preview` embrulham o spec renderizado em tema nenhum. Vale para todo builder que aceita cor, não só o botão.
  - **O modelo, definido pelo humano:** uma **área no projeto** para definir tema e tokens (espaçamento, estilos de texto, cores), com **cascata no estilo CSS** — o tema do **projeto** vale para todos os conteúdos; o que o **conteúdo** define sobrescreve o do projeto; o que o **widget** define sobrescreve os dois.
  - **Três coisas a decidir no discovery, anotadas agora para não se redescobrir:**
    1. **Onde a cascata é resolvida.** Tem de ser função pura do `sdui_core`, como o `resolveDrop` e o `diagnoseTree` — se editor e app resolverem cada um do seu jeito, o mock volta a divergir do aparelho, que é exatamente o defeito que este item corrige.
    2. **Token é referência, não valor.** Se o conteúdo sobrescrever copiando `#F0A020`, a cascata vira copiar-e-colar e perde o sentido; o que trafega deve ser nome semântico (`color.primary`, `space.md`, `text.body`) resolvido no fim da cadeia.
    3. **Tema de projeto × versionamento (item 24).** O conteúdo é publicado por versão, mas o tema mora no projeto: mudar a paleta muda **todo conteúdo já publicado sem republicar**. Isso é *feature* (é o que torna o tema útil) ou *risco*? A resposta aproxima este item das **variáveis remotas (33–35)**, que têm exatamente a mesma natureza.
  - **Precedência:** depois do **43** (que estabelece como o spec carrega chrome/tema de página) e **antes do Marco 8 (componentes)** — pelo mesmo argumento do item 30: componente criado antes dos tokens precisa migrar depois, um a um.
  - **Correção pequena que ficou de fora por decisão do humano (2026-08-17):** renderizar o mock e o `/preview` sob um tema Material neutro em vez do tema do editor mataria o sinal errado hoje, sem resolver o problema. O humano preferiu **não** fazer o paliativo e tratar o tema de verdade aqui.

## Marco 2 — Catálogo de widgets (track contínuo)

- `[-]` **9. Ampliar o catálogo usando o FlutterFlow como referência.** _(item -1)_ Track contínuo que alimenta paleta e Inspector; hoje **24 primitivos**. Novo primitivo = descriptor + builder + fixture, nada hardcoded no editor. → **[plano](plans/09-catalogo-widgets/plan.md)** (inclui o processo repetível "como adicionar um widget" + a fila priorizada de primitivos)
  - **Incremento 1 (entregue):** `textField`, `switch`, `checkbox`.
  - **Incremento 2 (entregue):** `textField` abrangente — `borderStyle`, `keyboardType`, `maxLength`, `prefixIcon`.
  - **Incremento 3 (entregue):** `radio`, `dropdown`, `slider`.
  - **Próximos incrementos:** ver a fila no plano.
  - `[x]` **Incremento 4: `image` abrangente** _(entregue com a F4 do item 39)_ — entrou junto com o item 39 (`alignment`, raio, `DimensionValue` em `width`/`height`, e o resto do básico que o `container` já tem e o `image` não).
- `[-]` **39. Widget `image` — a URL aparece, e o editor de propriedades cresce.** _(relato do uso real, 2026-08-15; 0-dep até a F2)_ Duas coisas no mesmo item, e o plano as separa: **o defeito** (informar uma URL não exibe a imagem e o editor **não diz por quê**) e **o catálogo** (o `image` tinha 4 propriedades contra 12 do `container` — **hoje tem 8** (`src`, `width`, `height`, `fit`, `alignment`, `borderRadius`, `backgroundColor`, `semanticLabel`), e o que falta do item é só a bateria (F5) — vira o **Incremento 4** do item 9). Docs vivas em `docs/16-image-url-e-props/`. → **[plano de gaveta](plans/39-image-url-e-props/plan.md)**
  - `[x]` **F1 — a imagem que falha para de mentir** _(2026-08-15; PR #128)_ O `errorBuilder` desenhava o mesmo quadrado cinza do estado "sem URL": falha de rede, host bloqueado por CORS e campo vazio eram pixel a pixel idênticos. Os três estados passam a se distinguir **por forma**. O carregando usa `frameBuilder`, não `loadingBuilder` — no Flutter Web `Image.network` não emite `ImageChunkEvent`, então o estado só existiria no mobile (D12). Nasce `SduiRenderer.showDiagnostics` (default `false`, só o editor liga): o renderer é o mesmo nos dois lados, e URL com token na query string não pode chegar à tela do usuário final — nem tirar o `src` bastava, porque o `toString()` da `NetworkImageLoadException` embute a URI (D13). Junto: `min: 0` → `min: 1` em `width`/`height` e o clamp com sinal no `NumberEditor`. **Não resolve o relato** — o host sem CORS continua falhando; carregá-lo é a F3.
  - `[x]` **F2 — proxy de mídia no backend** _(entregue; `51c9474`)_ ⚠️ **CISO obrigatório.** `GET /v1/media/proxy?url=` com allowlist de esquema, bloqueio de IP privado/loopback/link-local (`169.254.169.254` incluso), revalidação **a cada redirect**, teto de tamanho, timeout, validação de `Content-Type` e rate limit. Escolhido pelo humano sobre o `WebHtmlElementStrategy.fallback`, que viraria platform view **não capturável por screenshot** — e quebraria o próprio print que prova a correção. **O item 39 deixa de ser "não toca backend" aqui.**
  - `[x]` **F3 — o resolver no renderer + injeção no editor** _(entregue; `c5fb58d`)_. `SduiImageUrlResolver` opcional, no padrão do `showDiagnostics`: o editor injeta, o app cliente não. Sem isso, todo app publicado passaria a puxar imagem pelo nosso backend — banda e ponto único de falha, e inútil, porque em celular não há CORS (D11).
  - `[x]` **F4 — `image` abrangente** (o Incremento 4 do item 9) _(entregue; `9af0cbb`)_ · `[ ]` **F5 — bateria** — **é só isto que falta no item.**
- `[x]` **9b. Editores de propriedade avançados no Inspector (estados + binding).** Estados múltiplos por propriedade (`padding`/`margin`, `DimensionValue`, `AlignmentValue`) e **binding por propriedade** (`isBindable` + `SduiBinding` no kernel), enum como grupo de ícones, slider com faixa, "voltar ao padrão", seções colapsáveis e busca de propriedade.

## Marco 3 — Hierarquia Projeto → Categoria → Conteúdo (o fluxo do protótipo)

- `[x]` **9d. CRUD de Projeto — novo topo da hierarquia.** _(docs/09)_ Home de cards, upload de imagem (pipeline CISO aprovado), `Content.projectId` FK NOT NULL. **Débitos abertos, agora itens próprios do roadmap: auth (item 26) e storage Garage (item 27).**
- `[x]` **9e. Arquivar projeto (soft delete) + área de Arquivados.** Exclusão em duas camadas, cascata explícita em `$transaction`, confirmação dupla.
- `[x]` **9f. Projetos quebrado em homologação — corrigido e validado no hml.** Três quebras independentes do #43 (diálogo, Dockerfile, `express` não declarado); PRs #46/#47.
- `[x]` **9g. E2E reutilizável da hierarquia de Projetos.** `e2e_hml.sh` (18/18) + `e2e_shots.sh`/`e2e_drive.mjs` dirigindo a **homologação real**, não localhost.
- `[x]` **10. API de conteúdos com filtro, busca, ordenação e paginação + fundação de Categoria.** _(item 7; docs/08)_ Envelope `{data,nextCursor}` com cursor keyset, tabela `Category` em árvore, "Geral" por projeto.
- `[x]` **11–14. Tela do projeto:** árvore de categorias, "Todos os conteúdos" como entrada de topo, filtro por categoria e mover conteúdo entre categorias. _(itens 1, 6, 3, 2)_
- `[x]` **15. Busca e ordenação de conteúdos no painel.** _(item 4)_
- `[x]` **16. Listagem infinita (paginação por cursor).** _(item 5)_
- `[x]` **16b. Nome da categoria no card de conteúdo.** _(docs/11)_
- `[x]` **16c. AppBar global de duas faixas + breadcrumb (via `ShellRoute`).** _(PR #71)_ Chassi único no topo de todas as telas; mecanismo de **slot** em `core/widgets/app_shell/` — a página publica crumbs/ações como dados, o shell nunca lê cubit.

---

## Marco 4 — Ergonomia do construtor

_0-dep. Barato perto do ganho: é o que separa "dá para brincar" de "dá para trabalhar o dia inteiro"._

- `[x]` **23. Histórico do editor — desfazer/refazer, atalhos e duplicar/copiar/colar.** _(surgiu do uso; 0-dep)_ Entregue em `develop` nas quatro fases do plano: #111 (`cloneWithNewIds` no kernel), #112 (pilha de histórico no `EditorCubit`, com coalescing por chave, teto de 50 e reconciliação do status de salvamento), #116 (`EditorShortcuts` — `Ctrl+Z`/`Ctrl+Shift+Z`/`Ctrl+Y`/`Escape`, botões no topo e a guarda de foco), #114 (duplicar/copiar/colar) e #115 (bateria). De carona, dois defeitos que estavam no ar: `Delete` com o cursor num campo do Inspector apagava o **nó selecionado** (o `Shortcuts` do editor vence o `DefaultTextEditingShortcuts`), e o `updateSafeAreaProps` emitia fora do funil de mutação — `_emitDocument` passou a receber o `ContentSpec` inteiro, o que também é o que o item 29 precisa. **Pendente:** E2E manual em homologação (o merge em `develop` disparou o deploy). → **[plano](plans/23-historico-editor/plan.md)**
- `[x]` **38. Destravar o construtor — envolver um nó e drop sem beco sem saída.** _(relato do uso real, 2026-08-15; 0-dep)_ **É a metade que falta do item 8c.** Desde que a raiz virou livre, ela pode ser uma **folha** (`text`, `image`…), e o 8e fez o encaixe subir para o primeiro ancestral que aceita filhos — mas ninguém escreveu o que acontece quando a subida **não acha ancestral nenhum**: `resolveDrop` devolve `noSlotAvailable`, o gesto é recusado e a página fica sem saída. O dev não consegue criar o contêiner que faltava nem tirar de lá o widget que já está; a única saída é excluir a raiz, **que apaga o conteúdo inteiro sem aviso**. Entra a operação de **envolver um nó** no kernel (`wrapNode`), o comando explícito "Envolver em Column/Row" no editor, o `resolveDrop` devolvendo **`DropRequiresWrap`** em vez de recusar (o drop agrupa sozinho, desfazível num `Ctrl+Z`), a **marcação de problema no próprio nó** (árvore e canvas, hoje só no rodapé) e o rótulo honesto no excluir da raiz. Docs vivas em `docs/15-destravar-construtor/`. → **[plano de gaveta](plans/38-destravar-drop-e-envolver/plan.md)**
  - **As 6 fases estão em `develop`** _(2026-08-15)_, entregues como **stack de PRs** — #120 (`wrapNode`), #121 (comando + `Ctrl+G`), #122 (F3+F4, o drop agrupa; PR único por **VR-15-01**, porque `DropResolution` é `sealed` e o caso novo quebra os `switch` do cubit no mesmo instante), #123 (marcação no nó) e #125 ("Esvaziar conteúdo"). E2E instrumentado e verde contra a homologação real em #127.
  - `[x]` **E2E atestado pelo humano.** `Ctrl+G` num Chrome de verdade envolve o nó selecionado sem nenhuma interferência do navegador (sem busca, sem favoritar), e com o cursor num campo do Inspector não faz nada — os dois comportamentos que o teclado sintético por CDP não cobria.
  - `[x]` **F7 — bateria automatizada** _(2026-08-17)_. `wrapNode` e o braço `DropRequiresWrap` do `moveNode` (inclusive o teste de invariante varrendo o catálogo inteiro) já estavam cobertos no kernel; entraram 34 testes novos no editor — `wrapSelected` (comando explícito: nó do meio, raiz, os dois no-ops, notice `nodeWrapped`, `Ctrl+Z` numa entrada só, o aceite ponta a ponta F2→F4), `WrapNodeButton`/`InspectorHeader`, marcação de diagnóstico e "Esvaziar conteúdo" na `TreeRow`, e as mensagens `nodeWrapped`/`dropWrapped` da barra de status (inclusive o fallback silencioso do `wrapperLabel` para tipo fora do catálogo).
- `[-]` **41. Ergonomia do editor — o chrome sai da frente, e o conteúdo cabe na tela.** _(pedido do humano, 2026-08-15)_ Quatro coisas que o discovery mostrou serem de naturezas diferentes. Docs vivas em `docs/17-ergonomia-editor/`.
  - **O achado que reordenou tudo:** **não existe "ajustar à janela"** — o zoom do mock é manual (0,4–1,5). E com `Transform.scale` fixo, colapsar um painel libera 280px que viram **fundo cinza**: o mock não cresce um pixel. O fit não é só o maior ganho isolado do pedido — é **pré-requisito para o colapso e o modo tela cheia serem visíveis**.
  - **O editor não é responsivo hoje, e isso é bug, não polimento:** zero `LayoutBuilder`, chrome fixo em 612px. O breadcrumb estoura **a 1280** (ellipsis sem `Flexible`), o mock aparece cortado a 1024, e o preset **Tablet não cabe nem numa janela de 1600** (a `DeviceFrame` soma `bezel*0.55` de cada lado, mais 32 de padding do canvas — ~952px de centro). Pior: o fit do tablet exige escala **abaixo de 0,4**, que é o piso do `changeZoom` — por isso o fit **não pode** passar por ele.
  - **Metade do pedido já existe, faltando memória:** o Inspector já tem seções colapsáveis (item 9b), a paleta já agrupa (`WidgetCategories.inPaletteOrder`) e o `ResizableSplitView` já dá arraste. **Nada é lembrado** — o Inspector reabre expandido a cada troca de nó, a largura volta ao padrão a cada navegação.
  - **Nove fases de código em `develop`** _(status real, checado em `docs/17-ergonomia-editor/plan.md` §"Ordem de PRs" em 2026-08-17 — as duas linhas abaixo estavam desatualizadas)_: `[x]` **F1 — navegar no celular funciona** (#137) · `[x]` **F1b — abaixo de `compact` o editor cede o lugar a um portão** (#136) · `[x]` **F2 — rota `/preview/:projectId/:id`** (#135; QR **e** link copiável, que também serve quem abre o driva pelo próprio celular). **0-dep, sem backend, sem gate CISO** — escolhida sobre a via API pública, que custaria 3–4 fases com CISO e **quebraria quando o item 24 entrar** (lá `/v1/public` só serve publicado). Mostra o **último salvo**: salvar → refresh. · `[x]` **F4 — grupos da paleta colapsáveis** (#138) · `[x]` **F4b — o colapso sobrevive à troca de aba** (#141) · `[x]` **F2c — o `/preview` abre sem barra de endereços**, alvo PWA próprio (#142) · `[x]` **F2b — puxar para atualizar substitui a pílula-botão** (#143) · `[x]` **F3 — o piso do editor: overflows morrem + "ajustar à janela" + a status bar do mock** (#153) · `[x]` **F5 — painel colapsa em faixa fina de ícones** (#155) · `[x]` **F6 — layout lembrado** (larguras, painéis, grupos, Inspector; #159) · `[x]` **F7 — modo tela cheia** (#160, "última fase implementável").
  - **O que falta:** `[ ]` **F8 — E2E manual em homologação, atestado pelo humano** — QA instrumenta, mas o roteiro exige **os dois aparelhos físicos** (F1/F2) que só o dev humano tem; não é automatizável por CDP como o item 24. · `[ ]` **F9 — bateria automatizada + docs vivas**, bloqueada pelo F8 (regra do cap. 22 — nenhum teste antes do E2E atestado); escopo extenso já detalhado no plan.md (breakpoints, overflow em 13 larguras, goldens em faixa compacta, gate de rebuild). · `[ ]` **menu de destino ao soltar no corpo do nó** (item 5.3; discovery fechado em `specs.md`/`prd.md`, falta o tech-lead escrever as fases) · `[ ]` **nó inválido marcado como erro** (fase própria, só depois do menu — antes dele não há caminho que produza o estado). ⚠️ **Decisão do humano pendente, a ser feita quando a fase abrir — não antes:** um spec com nó inválido pode ser **salvo**, é **bloqueado**, ou é **salvo com aviso**?
  - **Recorte fixado pelo humano, não reabrir:** o `/preview` é *"um preview de como irá ficar no app de verdade, só isso"* — não é ambiente de teste real (esse será o app nativo apontando para HML). Melhorias de realismo além da pílula e da barra de endereços foram propostas e recusadas.
  - **Invariante I1:** o item 39 fez o `imageUrlResolver` viajar por 6 widgets até o `preview_surface.dart` — os mesmos arquivos que esta feature toca. **Uma rota de preview que esqueça de repassá-lo reintroduz o silêncio da imagem que o 39 acabou de matar.** Tem aceite próprio com print, não nota de rodapé.
  - **Não confundir com o item 30:** aquele é o **spec** ganhando variação por breakpoint (`SduiBreakpoint`, limiares 600/1024); este é o **editor** ser usável. Um `AppBreakpoints` do editor usaria os mesmos números **sem** importar o enum do kernel.
- `[x]` **42. Identidade visual: o ícone do app deixa de ser o do Flutter, e o nome próprio aparece.** _(pedido do humano, 2026-08-16; entregue 2026-08-17)_ **Quick win priorizado pelo humano em 2026-08-16, para logo após o item 41 fechar.**
  - **O nome, esclarecido pelo humano (2026-08-16):** **Driva Builder** é a aplicação; **Driva Editor** e **Driva Preview** são **funcionalidades** dela. Não há renomeação em cascata — pacotes, diretórios e repositório continuam `driva`/`driva_editor`. O que muda é só o nome **visível**: o todo se chama Driva Builder, e Editor/Preview qualificam a parte.
  - `[x]` **A metade de texto — entregue.** `c9e8ef7`/`8350ec7`, já em `develop`. `index.html`: `<title>Driva Builder</title>`, `apple-mobile-web-app-title: "Driva Builder"`, description real (não mais o texto genérico do template). Um script inline troca para "Driva Preview" quando o path começa com `/preview` (o iOS lê essas tags direto, ignora o manifest, ao "adicionar à tela inicial"). Os manifests já são **dois arquivos separados** (`manifest.json` do editor, `preview_manifest.json` do preview — confirma a suposição de "dois apps instaláveis" da F2c/D31): `name`/`short_name` corrigidos ("Driva Builder"/"Driva" e "Driva Preview"/"Preview").
  - `[x]` **O ícone — Concepto B aplicado (2026-08-17).** Escolha do humano entre os dois candidatos gerados (monograma **D+B**, Space Grotesk Bold): **Concepto B, "corte diagonal"** — tile com fundo em `conic-gradient` dividido na diagonal (`--icon-ink` #1C1512 × `--icon-accent` #E8602C), glifo "DB" num tom só (`--icon-paper` #F5F3F0). `web/icons/Icon-{192,512}.png` (cantos arredondados, 22%), `web/icons/Icon-maskable-{192,512}.png` (full-bleed, sem raio — a folga do "adaptive icon" vem do próprio Android recortando) e `web/favicon.png` (64×64) regenerados a partir da fonte real do projeto (`fonts/space_grotesk/SpaceGrotesk-Bold.ttf`), não um traço à mão — pixel fiel ao candidato aprovado no artifact "Monograma Driva Builder". Ícone único: os dois manifests (`manifest.json`, `preview_manifest.json`) já apontavam para o mesmo conjunto de arquivos, então nenhum deles precisou mudar.

## Marco 5 — Ciclo de vida do conteúdo (o gargalo do produto)

_Aqui o driva deixa de ser um editor e vira uma plataforma: alguém do outro lado passa a consumir o que foi montado._

> **Norte definido pelo humano em 2026-08-16:** depois do item 41 fechar e dos quick wins
> (item 42), o foco vira **habilitar teste num app de verdade** — 24 destrava, e o alvo
> real é a **fatia 2 do item 25** (o runtime `driva_client` empacotado). É isso que torna o
> sistema utilizável fora do editor.

- `[x]` **24. Publicação e versionamento do conteúdo.** _(2026-08-17; PR #161)_ O botão "Publish", até aqui um placeholder literal, virou o ciclo completo: **rascunho × publicado** (`draftSpec`/`ContentVersion`, rename sem perda de dado), **publicar** (idempotente, D3), **despublicar**, **rollback** (restaura para o rascunho, nunca republica direto, D4) e **histórico** paginado no editor. Fecha o débito VR-13-01: a API pública passa a servir a versão publicada, não mais o rascunho. E2E de contrato **103/103 PASS** e de UI **35/35 PASS** (18 prints, `docs/24-publicacao-versionamento/evidencias/rodada_01/`), os dois contra hml de verdade — inclusive os dois modos de falha (publish/restore) provados **visualmente distintos**, e a regressão do item 9e (exclusão de projeto com conteúdo publicado). Detalhes: `docs/24-publicacao-versionamento/final_report.md`. **Dois bugs pré-existentes, fora de escopo, descobertos pela validação em hml e registrados como itens 46 e 47.**
- `[-]` ⚠️ **50. Publicação e histórico: os botões saem do ⋮, e restaurar deixa de ser um salto no escuro.** _(pedido do humano, 2026-08-19; planejamento em [`docs/plans/50-historico-seguro/plan.md`](plans/50-historico-seguro/plan.md))_ Evolução direta do item 24, que entregou o **modelo conceitual certo** (rascunho mutável · versão imutável · publicado é um ponteiro — `docs/14-publicacao-versionamento/prd.md:73-87`) mas escondeu metade dele atrás de um menu e deixou "ver uma versão" sem porta própria. Quatro frentes; a auditoria de código de 2026-08-19 já separou o que é ergonomia do que é capacidade nova.
  - **(a) Despublicar e Histórico viram botões visíveis, e o ⋮ do editor some.** O `EditorMoreMenuDialog` (`apps/driva_editor/lib/modules/editor_module/presentation/editor/widgets/publish/editor_more_menu_dialog.dart`) tem **exatamente dois itens** — "Ver histórico de versões" e "Despublicar" —, então promover os dois **esvazia o menu**: a ação `Icons.more_vert` de `editor_top_registrar.dart:129-138` deixa de ter razão de existir e sai junto. Frente **só de UI, sem backend**: a confirmação de despublicar **já existe** (`unpublish_confirm_dialog.dart`) e o **404 no app cliente também** — a API pública serve `publishedVersionId`, então conteúdo fora do ar já não aparece (o item 24 fechou o débito VR-13-01). **Armadilha que o plano tem de tratar:** existe um **segundo** `more_vert`, o overflow genérico do shell (`core/widgets/app_shell/app_shell_actions_overflow_menu.dart:9`), montado abaixo de 760px (`core/theme/app_sizes.dart:111`), que recolhe **tudo menos a primeira ação `filled`**. Hoje isso já produz, em tela estreita, um ⋮ cujo item "Mais opções" abre **outro** menu — e dois botões a mais na barra pioram a faixa compacta se ninguém olhar para ela.
  - **(b) Ver uma versão sem restaurá-la — o buraco real.** Hoje a **única** porta para uma versão antiga é o botão "Restaurar" (`version_row.dart:91-94`), e ele **sobrescreve o rascunho de verdade**: `contents.service.ts:354-361` grava o `draftSpec` no banco. Quem tinha trabalho não publicado o perde só para *olhar* uma versão — e o Ctrl+Z do editor (`editor_cubit.dart:591-624`) desfaz **só localmente**, sem reverter o servidor até um novo save. O pedido do humano ("seleciona no histórico, aparece no preview, e só publica quando clicar em Publicar") é exatamente esse conserto. **O backend já está pronto:** `GET /v1/contents/:id/versions/:version` existe e é o **único** endpoint que devolve o `spec` (`contents.controller.ts:53-60` → `contents.service.ts:332-345`) — e **nenhum código Flutter o consome**. O que falta é tudo do lado Dart: `ContentVersion` (`domain/entities/content_version.dart`) **não carrega `spec`**, o `EditorRepository` não tem método para buscar uma versão, e o preview (`preview_page.dart:26-27`) aceita só `projectId`/`id`, renderiza o `draftSpec` via `LoadContentUseCase` e **não tem caminho para spec arbitrário** — sem query param, sem `extra`, sem rota de versão.
  - **(c) A versão publicada sempre visível — e um débito do item 24 que a auditoria achou.** ⚠️ A decisão **A3** do item 24 (`docs/14-publicacao-versionamento/prd.md:593-609`) mandava expor **`latestVersion: int | null`** no `GET /v1/contents/:id` **e** no `toSummary`, para separar "fora do ar" de "nunca publicado". **Nunca foi implementada** — `latestVersion` tem **zero ocorrências** em `backend/src/` e em `apps/driva_editor/lib/`; `ContentsService.find` (`contents.service.ts:165-179`) devolve só `publishedVersion` e `hasUnpublishedChanges`, e a entidade `PublicationState` não tem o campo. Consequência nos **três lugares que a própria A3 nomeou**: o topo do editor cai no ramo `!isPublished` e anuncia **"Nunca publicado"** para um conteúdo despublicado (`editor_top_registrar.dart:257-263`); o selo da lista diz **"Rascunho"** e o tooltip repete "Nunca publicado" (`publication_badge.dart:22-31`); e a confirmação de despublicar ficou no genérico "O histórico de versões continua guardado" em vez da contagem de versões que a A3 pedia. O estado **"Fora do ar (última: vN)"** de `docs/14-publicacao-versionamento/specs.md:111-141` **não existe na tela** — o usuário despublica e a interface sugere que o histórico sumiu, o oposto exato do que a confirmação promete. **Junto entra o pedido do humano:** o número da versão no ar aparecer também no estado "Alterações não publicadas" (o rótulo hoje perde o `(no ar: v3)` que a spec pedia) e no selo da lista, que não mostra versão nenhuma.
  - **(d) Comparar versões lado a lado, antes de decidir — e o que o Squidex ensina.** O humano pediu para checar como o **Squidex** faz, e a resposta muda o desenho: o `Compare` de lá **não é diff textual de JSON**, é **campo a campo com merge seletivo**. Cada campo renderiza em duas colunas (atual à esquerda, versão antiga à direita, `col-6`/`col-6`), um observable marca só os que diferem e, no campo que difere, aparece um botão **copiar de volta** que traz **aquele campo** da versão antiga para o formulário atual. O ganho não está em *ver* a diferença — está em **aproveitar um pedaço da versão antiga sem engolir o resto**. Traduzido para o driva, a unidade não é o campo, é o **nó da árvore** (`id`/`type`/`props`): duas colunas, nós adicionados/removidos/alterados marcados, e a ação "trazer este nó para o rascunho". Coluna A default = a versão **no ar**; coluna B = a escolhida no histórico. Padrão do repositório: cor **nunca** é o único sinal (`CLAUDE.md`) — cada marca é ícone + rótulo além da cor. **Nota de custo, dita pelo próprio autor do Squidex:** a comparação foi **recusada em 2018 por complexidade** ("all the different field types and editors") e só entrou na 6.0.0, em 2021 — é a frente mais cara das quatro, e a única que sobrevive sozinha se for adiada.
  - **A mecânica de segurança — a pergunta que o humano fez, respondida.** O modelo do item 24 já é o correto e **não deve ser mexido**: histórico é **append-only**, e restaurar **não reescreve o passado**. Restaurar a v2 e publicar **não faz o ponteiro voltar para a v2** — cria a v7 com o conteúdo da v2, e o histórico registra a linhagem. Isso mantém a auditoria linear e garante que o app cliente nunca receba algo que ninguém revisou. O que falta é **separar os três verbos que hoje se confundem num botão só**: **ver** (leitura pura, efeito zero — não existe), **trazer para o rascunho** (o `restore` atual, destrutivo do rascunho, hoje é a única porta) e **publicar** (sempre explícito, sempre versão nova). Com os três separados, "restaurar uma versão" vira: abre o histórico → escolhe → **vê no preview** sem tocar em nada → compara com o que está no ar → traz para o rascunho **se quiser** → revisa no canvas → publica. Cada passo reversível até o último.
  - **Rede de segurança contra perda, a decidir no discovery.** O `restore` sobrescreve rascunho sujo com um aviso genérico. Duas saídas: **carimbar o rascunho atual como versão** antes de sobrescrever (nada se perde, mas polui o histórico com versões que ninguém publicou), ou **nomear na confirmação o que será perdido** (mais barato, mas continua destrutivo). ⚠️ **Decisão do humano, quando a fase abrir.**
  - **Procedência do desenho — quatro CMSs, um padrão convergente.** Levantado em 2026-08-19. **Squidex** (event sourcing, append-only) **não tem operação "restaurar"**: a mecânica é **`Load` + `Save`** — o botão `Load` na linha do histórico carrega a versão antiga **no próprio editor** com uma faixa vermelha *"Viewing version {N}"* e um botão *"View latest"*, e só o save seguinte vira versão nova. **Contentful** sobrescreve a entry atual e o snapshot novo nasce no publish seguinte; **Strapi 5** é explícito — *"the content of this version will override the content of the current draft version"*; **Sanity** grava a reversão como mutação nova, histórico intacto. **Nos quatro, restaurar mexe no estado de trabalho e o histórico nunca é reescrito.** É exatamente o que o `restore` do driva já faz (`contents.service.ts:354-361` grava o `draftSpec`, D4 do item 24) — **o modelo está certo e não se mexe nele**. O que o driva não tem é o **`Load` isolado**: aqui o "ver" e o "trazer para o rascunho" são o mesmo botão, e é essa fusão que torna olhar uma versão um ato destrutivo. **Onde o driva já sai na frente:** o "New Draft" que o Squidex precisa criar e apagar sob permissão dedicada (`POST/DELETE .../draft`, só sobre conteúdo publicado) existe aqui por construção — o `draftSpec` está sempre lá, separado do ponteiro publicado.
  - **Estado (2026-08-20).** Tarefas T1 a T4 em `develop` (PRs #185, #186, #188, #189). **T5 no PR #191**, aguardando merge: comparação lado a lado com preview dos dois lados e cópia de propriedades pelo motor puro. Faltam **T6** (provar o ciclo imutável e atualizar docs), **T7** (E2E humano em homologação) e **T8** (bateria e encerramento). Desvios aprovados e a correção de um DoD impossível: [`variance_report.md`](plans/50-historico-seguro/variance_report.md).
  - ⚠️ **Duas decisões esperando o humano (abertas em 2026-08-20, na T3/T5).** **(i)** O `VersionReviewDialog` deve ganhar um botão "carregar no rascunho" dentro dele (fluxo revisar-depois-carregar)? Hoje a ação só existe na linha do histórico, e quem abriu a revisão precisa fechá-la para carregar. **(ii)** `AppSizes.versionReviewDialogFitWidth = 900` e `versionReviewDialogWidth = 720` foram escolha de quem executou a T3, não do plano — merecem um passe de design antes do E2E congelar os prints.
  - **Precedência:** depende só do item **24** (entregue). **Não depende do 25** e toca arquivos disjuntos da fatia 2 dele (`packages/driva_client` nasce do zero; este item vive em `apps/driva_editor` + `backend/src/contents`) — **os dois podem correr em paralelo**.
- `[-]` **25. Entrega ao app cliente — API pública de leitura, runtime SDK e app de exemplo.** `DrivaContent` era um `throw UnimplementedError` no `sdui_flutter`; a fatia 2 o tirou de lá e o package `driva_client` passou a implementá-lo de verdade. Entra o `GET` público por slug (só versão **publicada**, com `ETag`/cache e chave publicável por projeto), o runtime (`Driva.init` + `DrivaContent(slug:)` com cache em disco e fallback embarcado) e um **app de exemplo** que prova o ciclo fim-a-fim. → **[plano](plans/25-entrega-app-cliente/plan.md)** **Fronteira com o item 43 (decidida em 2026-08-17):** a remoção da casca do app de demonstração **na tela de conteúdo** saiu deste item e foi para o 43 (F0). **A F0 encolheu (2026-08-19):** o `content_meta_bar.dart` já não existe e o `bottomNavigationBar` já é outra coisa (`ContentSlugBar`) — sobrou só tirar o `appBar:` de `content_page.dart`. ~~Continuam aqui: apagar o arquivo `content_meta_bar.dart` e a tela de catálogo~~ — **os dois foram apagados pela F2 da fatia 2 (2026-08-19)**, junto com `published_module/data` e o `domain/` que só o servia.
  - `[x]` **Fatia 1 — API pública + app de demonstração** _(2026-08-15; docs/13-loop-sdui)_ **O loop SDUI fechou:** `GET /v1/public/contents[/:slug]` com **chave publicável** (`Project.publishableKey`, header `x-driva-key`), `ETag`/`304` e CORS só nesse prefixo; `apps/driva_demo_app` consome e renderiza com `SduiView` — primeiro consumidor externo do renderer. E2E de contrato 17 PASS/0 FAIL + prints do app renderizando. **Desvio registrado:** foi entregue **antes do item 24**, então serve o rascunho e não uma versão publicada (`variance_report.md` VR-13-01) — não abrir para cliente real antes do 24.
  - `[-]` **Fatia 2 — runtime empacotado (`driva_client`)**: cache em disco, *stale-while-revalidate*, fallback embarcado e degradação quando o `specVersion` do servidor é mais novo que o do app. **O consumo real elegeu isto como o mais perigoso** (app na loja não se atualiza). Depende do item 24. **Código entregue em 2026-08-19; falta o E2E e a bateria.**
    - `[x]` **O package existe** — `packages/driva_client`: `Driva.init` + `DrivaContent(slug:)`, resolução **memória → disco → rede** com revalidação em segundo plano (um `304` não redesenha), todo JSON passando por `parseContentSpec` (inclusive o do disco: cache corrompido é apagado e a resolução recomeça), fallback embarcado e a garantia de que **nenhuma exceção sobe para a tela do cliente**. Chave de cache derivada da `publishableKey`, então dois projetos no mesmo app não se misturam. `http` com `Client` injetável, não `dio` — é package de terceiro. `DrivaContent` **saiu do `sdui_flutter`** (breaking, 0.3.0 → 0.4.0): o renderer volta a fazer uma coisa só.
    - `[x]` **O app de demonstração é o primeiro cliente** — a migração apagou `published_module/data/`, o `domain/` que só o servia, e `core/error/`+`core/network/`: **175 linhas somadas contra 1292 removidas**. Era a prova pretendida — se o package não desse conta, a migração não fecharia. A tela de catálogo saiu junto (listar o projeto inteiro só serve a um app de demonstração), e com ela quem escolhia o slug: o app abre um `DEFAULT_SLUG` de config, com campo para trocar e botão de recarregar.
    - `[x]` **Rate limit da rota pública** (D7 do plano, débito que a tabela de vigilâncias atribuía a este item): 120 req/min. **O primeiro veredito foi reprovado e valeu a pena** — `ThrottlerModule` é `@Global()` e três `forRoot` disputavam o token, então a rota aplicava **60/min, não 120**, e o teste que a acompanhava montava só o `PublicModule`, ficando verde sobre um limite que a produção não aplicava. Corrigido com `@Throttle` no handler, no padrão que `projects.controller.ts` já usava.
    - `[x]` **Guia de integração** (`docs/25-entrega-app-cliente/integracao.md`) — inclusive a seção do `specVersion` incompatível, que é o cenário que mais assusta em SDUI.
    - `[ ]` **F4 — E2E do ciclo completo contra homologação, atestado pelo dev humano.** ⚠️ **Tem pré-requisito:** a migração apagou o `missing_key_view.dart`, então chave placeholder/errada e falta de internet produzem **a mesma tela** — dois modos de falha, um estado visual. Corrigir antes de rodar o roteiro (registrado na R9 do plano).
    - `[ ]` **F5 — bateria automatizada**, bloqueada pela F4 (regra do cap. 22). Escopo conhecido: os 4 testes do app que a migração levou junto, e cobertura de widget para `ContentPage`/`ContentSlugBar`, que hoje não existe.
    - **Saiu antes desta fatia, como bugfix próprio _(2026-08-17)_:** a permissão `android.permission.INTERNET` faltava no manifest de `main` do app de demonstração — **todo build release saía sem acesso à rede**. Não esperou o runtime empacotado porque é uma linha e bloqueia qualquer teste em Android físico. Junto veio a **receita de build release** (`tool/run_demo.sh` em `MODE=apk`), que faltava no repo: os `config/<env>.json` versionados trazem `PUBLISHABLE_KEY` de placeholder, então um release gerado por `--dart-define-from-file` compilava e mostrava vitrine vazia (404).
    - **O ponto cego que deixou o defeito acima viver foi fechado no mesmo dia _(2026-08-17)_:** a CI rodava `flutter analyze` + `flutter test` e **nenhum `flutter build`** — nada fora do Dart passava pela cancela. Entrou o job `Android — build do app de demonstração` (`flutter build apk --debug`), que pega regressão de Gradle/AGP/template. O manifest em si segue guardado por teste (`test/android_manifest_test.dart`), e não pelo build: o template do Flutter injeta `INTERNET` no manifest de debug, então **nenhum** build de debug reprovaria por falta dela.
    - **Continua parked para esta fatia:** o que sobra da branch `parked/demo-app-internet-e-casca` depois da fronteira acima — **apagar** o arquivo `content_meta_bar.dart` e a tela de catálogo. Tirar o `appBar:` e o `bottomNavigationBar:` da tela de conteúdo não espera mais esta fatia: virou a **F0 do item 43** (decisão de 2026-08-17), porque é lá que a barra de topo passa a vir do spec.

## Marco 6 — Produção de verdade

_Débitos assumidos por decisão do humano em 2026-07-09 (`docs/09-crud-projeto/variance_report.md`). O limite registrado: **auth entra antes de abrir para usuários reais**._

- `[ ]` **51. Instalar o app no celular a partir do editor — link/QR do APK ao lado do preview no navegador.** _(pedido do humano, 2026-08-20)_ Hoje a janela de preview oferece só o caminho do navegador. A ideia é oferecer, ao lado dele, o APK do app para instalar no aparelho — ver o conteúdo num app de verdade, não numa aba.
  - **O que já existe e não precisa ser refeito.** O editor já compartilha o preview por **QR code** (`apps/driva_editor/lib/modules/editor_module/presentation/editor/widgets/canvas/preview_share_dialog.dart`, com `qr_flutter`), então a superfície de UI já está montada. O preview web já é **instalável como PWA** ("Driva Preview", `preview_manifest.json`). O app já builda APK por `tool/run_demo.sh MODE=apk`, e a CI já tem job Android.
  - ⚠️ **A armadilha que decide o desenho: a chave é compile-time.** `publishableKey` e `defaultSlug` do app vêm de `String.fromEnvironment` (`apps/driva_demo_app/lib/core/config/app_config.dart:19-24`) — ficam **assados no binário**. Um APK genérico linkado no editor abriria sempre o projeto cujo `pk_` foi embarcado: quem baixasse a partir de outro projeto veria conteúdo alheio ou 404, sem erro que explique. E o app **não tem deep link** — o `AndroidManifest.xml` só declara `MAIN`/`LAUNCHER`.
  - **Três saídas, com o custo de cada uma.** **(a) APK genérico + configuração em runtime** _(recomendada)_: o app passa a receber `key`/`slug` por deep link (App Link `https://` ou esquema próprio) ou lendo o QR que o editor já sabe desenhar; um binário serve todos os projetos e o segredo nenhum é embarcado. Exige mexer no app (config em runtime + intent-filter) e no editor (o QR de configuração ao lado do link). **(b) APK por projeto, buildado sob demanda**: é o mais fiel ao que o cliente veria, mas pede job de CI por clique, storage por projeto e minutos de espera. **(c) APK fixo de demonstração**: barato, porém mostra o projeto default e não o trabalho de quem clicou — engana mais do que ajuda.
  - **iOS fica de fora por plataforma, não por escopo.** Sideload de IPA exige TestFlight ou distribuição ad-hoc com UDID registrado. Para iPhone o caminho continua sendo o preview web instalável, que já existe — e a tela deve **dizer isso**, em vez de deixar o usuário procurar um botão que não vem.
  - **Distribuição é metade do trabalho.** O buildType release do app assina hoje com a **chave de debug**, de propósito (não há keystore no repositório). Para distribuir por link é preciso keystore próprio — sem ele, cada build novo obriga a desinstalar o anterior —, um lugar para hospedar o binário (GitHub Releases ou o backend) e uma forma de o editor descobrir a URL da versão corrente. Instalação por fonte desconhecida continua sendo um passo manual do usuário no Android; a tela precisa explicá-lo.
  - **Precedência:** independe do item 50. Encosta no item 25 (o app já consome o `driva_client`). Toca `apps/driva_editor` (janela de preview), `apps/driva_demo_app` (config em runtime + deep link), a CI (build release assinado e publicação) e possivelmente o `backend` (endpoint com a URL/versão do APK). ⚠️ **Decisão do humano no discovery:** qual das três saídas — e o quanto disto é preview de trabalho *versus* vitrine de demonstração, que é a pergunta que separa (a) de (c).
- `[ ]` **26. Autenticação e multi-tenant real.** Hoje o "auth" é o header `x-project-id` — qualquer um que saiba um id lê e escreve o projeto inteiro. Entra usuário/sessão, vínculo usuário↔projeto e o escopo de tenant deixando de vir do cliente. Impacta **todos** os controllers do backend e o `DioClient`/`ProjectScope` do editor. **Herda o débito do item 46** (2026-08-18): matar o singleton `ProjectScope` — `projectId` explícito em repositório/use case, e o fim do `DEFAULT_PROJECT_ID` compilado no binário. → **[plano](plans/26-auth-multi-tenant/plan.md)**
- `[ ]` **37. SaaS: organizações, permissões, chaves e webhooks.** _(pedido do humano, 2026-08-14)_ O que transforma o item 26 em produto multi-cliente: **organizações** com convite de usuários e **papéis** (quem edita, quem só vê, quem publica), **gestão das chaves de acesso** (a `publishableKey` já existe desde a fatia 1 do item 25 — falta rotação, revogação e chave por ambiente), a **área de administração** onde isso tudo é operado, e **webhooks** para avisar sistemas de fora quando algo acontece (conteúdo publicado, variável alterada, experimento iniciado), com entrega assinada, reentrega em falha e log. Depende do item 26. ⚠️ **precisa de refinamento**, com **CISO obrigatório** em cada fase.
- `[ ]` **27. Storage S3/Garage ligado.** Mais perto do que o texto sugeria (auditoria de 2026-08-19): o **adapter S3 já está codado e fiado** — `backend/src/storage/s3-storage.service.ts` existe e o `storage.module.ts:15-30` já troca de driver por `STORAGE_DRIVER`. **Não falta implementar, falta ligar:** apontar para o **Garage** (`s3.bmjtech.duckdns.org`), com key `<projectId>/midias/<uuid>.<ext>` e credenciais só via env no Coolify. Depende do item 26 (decisão registrada). → **[plano](plans/27-storage-garage/plan.md)**

## Marco 7 — Interatividade (o spec deixa de ser estático)

- `[ ]` **28. Eventos e ações editáveis no Inspector.** `SduiAction` e o `renderer.dispatch` existem no kernel, mas **só o `button` dispara** e **nada no editor edita eventos** — um botão montado no editor não faz nada. Entra o catálogo de eventos por widget (no `WidgetDescriptor`), o catálogo de ações (navegar, abrir URL, mostrar mensagem, chamar API), o editor de eventos no Inspector e o dispatch no renderer. Depende do item 25 para ter sentido de ponta a ponta (é o app cliente quem executa a ação). → **[plano](plans/28-eventos-acoes/plan.md)**
- `[ ]` **29. Contexto de dados e binding com contrato.** `{{expressão}}` já é reconhecida e editável (item 9b), mas nada define **o que existe** para bindar: o campo é texto livre e o renderer não resolve. Entram os **parâmetros da página**, a **fonte de dados** por conteúdo e a resolução do binding no renderer, com o Inspector oferecendo as chaves disponíveis em vez de texto livre. Depende do item 28. → **[plano](plans/29-contexto-de-dados/plan.md)**
- `[ ]` **31. Fontes de dados: API como origem de propriedades.** _(pedido do humano, 2026-08-14)_ Declarar no editor uma chamada HTTP (URL, método, cabeçalhos, auth) e **mapear** a resposta para o contexto de dados, para o conteúdo exibir dado real (lista de produtos, nome do usuário, saldo). Inclui estado de carregando e de falha. É o item 29 puxado até a origem: lá se define **o que existe** para bindar; aqui, **de onde vem**. ⚠️ **precisa de refinamento** — **CISO obrigatório**: segredo de API nunca pode descer para o app.
- `[ ]` **32. Gatilhos e ciclos de vida.** _(pedido do humano, 2026-08-14)_ Os "lugares" onde algo dispara, espelhando o modelo mental do Flutter, em quatro escopos: **app** (ao iniciar), **projeto**, **conteúdo** (ao abrir, antes do build, depois do build, ao fechar) e **componente** (init, dispose). Um gatilho amarra um momento a uma ação do item 28 — "ao abrir o conteúdo, buscar a fonte X e preencher o contexto". Sem isso, ação só existe presa a toque de widget. Depende dos itens 28 e 31. ⚠️ **precisa de refinamento.**

## Marco 7b — Variáveis remotas e testes A/B (o driva vira ConfigCat + Firebase A/B)

_Pedido do humano em 2026-08-14, com a referência declarada: **ConfigCat** (variáveis remotas) e **Firebase A/B Testing** (experimentos). Depende do runtime existir (item 25) — variável só vale se alguém a resolve no cliente — e do contexto de dados (item 29), que é onde ela aterrissa._

> ⚠️ **Todo este marco precisa de refinamento.** Decisões de produto pesadas; nada entra em implementação antes de discovery + specs/prd.

- `[ ]` **33. Variáveis remotas por projeto e por conteúdo.** Chave/valor **tipado** (bool, número, texto, JSON) com valor padrão, definidas no editor e entregues ao cliente junto do spec. Dois escopos: variável do **projeto** (vale para tudo) e do **conteúdo** (sobrescreve). Muda o comportamento do app **sem republicar** — é o que separa "spec estático" de "configuração viva". Base dos itens 34–35. ⚠️ refinar.
- `[ ]` **34. Variável como valor de propriedade.** Qualquer prop `isBindable` passa a aceitar uma variável como origem, na mecânica de `{{...}}` que o Inspector já tem (item 9b), resolvida em runtime; o editor mostra o **valor padrão** no preview e sinaliza que aquilo é dinâmico. Depende dos itens 33 e 29. ⚠️ refinar.
- `[ ]` **35. Testes A/B sobre as variáveis.** Experimento = **variantes** de uma ou mais variáveis + regra de distribuição + público-alvo. O difícil não é sortear: é a **consistência** (o mesmo usuário vê sempre a mesma variante, inclusive offline e entre sessões), a **unidade de sorteio** (usuário? dispositivo? instalação?), a convivência de experimentos simultâneos e a métrica que diz quem ganhou. Depende do item 33. ⚠️ refinar — provavelmente vira marco próprio no refinamento.

## Marco 8 — Componentes (widgets reutilizáveis)

_A maior frente. Depende do construtor maduro (Marcos 1–2, 4) e da hierarquia (Marco 3). Ganha muito se vier depois do ciclo de publicação (24/25), porque componente publicado tem os mesmos problemas de versão._

- `[ ]` **19. Home passa a exibir Conteúdos e Componentes.** _(item 17)_ Divisão de nível superior entre as duas coisas dentro da tela do projeto. → **[plano](plans/19-home-conteudos-componentes/plan.md)**
- `[ ]` **20. Componente como widget reutilizável, com construtor próprio.** _(item 18)_ Mesma premissa de Conteúdo; um componente é um widget que poderá ser usado dentro de um conteúdo. Depende do item 19. → **[plano](plans/20-componente-construtor/plan.md)**
- `[ ]` **21. Aba "Componentes" no editor, ao lado de Widgets e Árvore.** _(item 19)_ Componentes do projeto ficam disponíveis para uso no construtor de conteúdo, com instância por referência. Depende do item 20. → **[plano](plans/21-aba-componentes-editor/plan.md)**
- `[ ]` **22. Metadados e listagem de componentes no padrão da lista de Widgets.** _(item 20)_ Ao salvar um componente, escolher categoria e definir ícone/imagem, para ele aparecer bonito na lista como os widgets. Depende dos itens 20 e 21. → **[plano](plans/22-metadados-componente/plan.md)**
- `[ ]` **36. Área exclusiva de construção de componentes personalizados.** _(pedido do humano, 2026-08-14)_ Um espaço dedicado — não o construtor de conteúdo com outro chapéu — para criar o componente, definir suas **propriedades públicas** (o que quem usa poderá configurar), pré-visualizar em isolamento e publicá-lo na lista. A decisão central é a modelagem dessas props públicas: é o **contrato** do componente, e é o que decide se ele aparece na paleta como um primitivo de verdade. Depende dos itens 20–22. ⚠️ **precisa de refinamento.**

## Marco 9 — Offline-first

> **Correção do humano (2026-08-14):** offline-first e pull-to-refresh são, antes de tudo, do **SDK do cliente** — é o app do cliente que precisa abrir sem rede e revalidar. Os itens 17/18 abaixo continuam valendo para o **editor** (a lista de conteúdos), mas a versão que importa para o produto é a do runtime, e ela vive na **fatia 2 do item 25**.

- `[ ]` **17. Offline-first na tela de conteúdos (editor).** _(item 8)_ Cache local da lista; atualiza ao salvar. Depende da lista já com filtro/busca/paginação (itens 13–16, entregues) e reaproveita a persistência do item 3. → **[plano](plans/17-offline-first/plan.md)**
- `[ ]` **18. Pull-to-refresh que refaz o cache (editor).** _(item 9)_ Depende do item 17. → **[plano](plans/18-pull-to-refresh/plan.md)**

## Track contínuo — encaixáveis a qualquer momento

- `[ ]` **30. Responsividade — o spec ganha variação por breakpoint.** _(surgiu da análise; 2026-08-13)_ O mock tem presets de dispositivo, mas o spec descreve **uma** tela: o mesmo JSON vai para celular e tablet sem alternativa. Entra o conceito de override por breakpoint em props selecionadas, com o canvas mostrando qual breakpoint está sendo editado. Independente, mas **mais barato antes** dos componentes (item 20) — depois, cada componente precisaria migrar junto. → **[plano](plans/30-responsividade-breakpoints/plan.md)**
- `[x]` **44. Título do seletor de cor vaza para fora do modal.** _(relato do uso real, 2026-08-16; corrigido 2026-08-17)_ O `title: const Text('Selecionar cor')` passado a `showPickerDialog` (não o `heading` do `ColorPicker`, que não era usado) estourava a largura do modal. `title` é opcional na API do `flex_color_picker` — `null` (o padrão) já omite a faixa de título completamente, sem regressão: o diálogo continua com `dialogTitle`/`dialogCancelButtonLabel` em pt-BR e o preview/swatch deixa claro o que é.
- `[x]` **46. Recarregar a página do editor num projeto que não é o `default` mostra "Conteúdo não encontrado".** _(achado pela validação do E2E do item 24 contra hml, 2026-08-17; entregue 2026-08-18)_ `EditorRoutes.editor = '/contents/:id/edit'` **não carrega o `projectId` na URL** — `EditorPage.pageBuilder` lê `getIt<ProjectScope>().projectId` (`core/network/project_scope.dart`), um singleton **só em memória**, sem persistência nenhuma (nem `localStorage`, nem query param). `ProjectScope` só é estampado corretamente quando o usuário **navega pelo app** a partir da tela do projeto (`ProjectDetailPage.pageBuilder` faz `getIt<ProjectScope>().projectId = id`). Qualquer recarregamento de página nessa rota — F5, link direto, aba nova, ou (o que expôs o bug) um `Page.navigate` de driver E2E — reboota o app e `ProjectScope` volta ao `AppConfig.defaultProjectId` (`'default'`), então o `GET /v1/contents/:id` sai com o `x-project-id` **errado** para qualquer projeto que não seja esse. O servidor responde 404 (escopo por tenant, correto do lado dele) e a tela mostra "Conteúdo não encontrado" — mensagem enganosa, o conteúdo existe. **Só não afeta o projeto `default`** porque, por coincidência, é ele o valor de fallback. **Correção provável, a confirmar no discovery:** a rota precisa carregar o `projectId` (`/projects/:projectId/contents/:id/edit`, no padrão do `/preview/:projectId/:id` que já existe) em vez de depender de estado em memória — mesma classe de problema que a nota de `editor_routes.dart` já resolveu para o preview. **Discovery feito (2026-08-17): `docs/46-projectid-na-rota-do-editor/`** (`specs.md` + `prd.md`). Confirmou a causa, e achou **um modo silencioso** que o relato não cobria: o link "ver no celular" (`canvas_area.dart:82`) sai `/preview/default/<id>` depois de um reload, **sem erro nenhum na tela** — o usuário copia um link quebrado. Custo estimado: **1 PR**. **Decisões travadas pelo humano em 2026-08-17** (`specs.md`/`prd.md` › _Decisões do humano_): **A1** rota nova `/projects/:projectId/contents/:id/edit` (recomendação seguida) · **A2** link antigo (`/contents/:id/edit`) **remove a rota** — cai no `onException` padrão e vai para a home **sem** aviso dedicado (opção mais barata, **não** a recomendada pelo PM/tech-lead, que era avisar antes de mandar para a home) · **A3** a tela de falha também para de mentir — separa "projeto não existe" de "conteúdo não existe neste projeto", sem request novo (recomendação seguida) · **A4** correção pontual no padrão da D18, 1 PR (recomendação seguida); matar o singleton `ProjectScope` fica registrado como débito vivo apontando para o item 26. **Entregue em 2026-08-18** (`docs/46-projectid-na-rota-do-editor/`): rota nova, escopo carimbado no `pageBuilder` antes da carga, tela de falha com as três saídas da D7, E2E em homologação atestado pelo dev humano (`final_report.md`) e bateria automatizada cobrindo rota, escopo, matriz de falha e os dois pontos de navegação. **Débito que fica:** o escopo de projeto continua vindo do singleton `ProjectScope` — matá-lo (`projectId` explícito em repositório/use case) toca `data` e `domain` de todos os módulos e é trabalho do **item 26**, que vai remexer nessa camada de qualquer jeito; junto dele sai o `DEFAULT_PROJECT_ID` compilado nos quatro flavors.
- `[ ]` ⚠️ **47. Criar conteúdo sem `categoryId` explícito falhou em homologação — e a causa registrada estava errada.** _(achado pela validação do E2E do item 24, 2026-08-17; **causa reescrita em 2026-08-19 depois de auditoria contra o código**)_ **O sintoma é real:** `POST /v1/contents` sem `categoryId` devolveu 400 (`"projeto sem categoria \"Geral\" — contate o suporte"`) no projeto `default` de hml. **A explicação anterior não era:** dizia que o fallback busca a categoria "pelo nome literal Geral" e que renomear a quebrava. O fallback busca por **slug** — `GERAL_SLUG = 'geral'` (`backend/src/contents/contents.service.ts:21`, usado em `:403-406`) — e `CategoriesService.update` grava **só `name`** e `parentId`, nunca recalculando o slug (`backend/src/categories/categories.service.ts:88-90`). **Renomear "Geral" para "Divulgar" não quebra nada.** Quem for fazer o discovery precisa partir daqui, não da hipótese antiga.
  - **Dois caminhos de quebra que o código de fato permite**, achados na auditoria: **(a) apagar** a categoria default — `CategoriesService.remove` só recusa quando há conteúdo ou subcategoria (`categories.service.ts:113-122`), e nada protege a default como tal; **(b) movê-la para dentro de outra** — a query do fallback exige `parentId: null` (`contents.service.ts:404`), e o `update` permite trocar o `parentId` (`categories.service.ts:89`). O caminho (b) não estava em lugar nenhum e é o mais silencioso: a categoria continua existindo, com o slug certo, e o fallback deixa de achá-la.
  - ⚠️ **A causa do 400 em hml continua indeterminada.** A auditoria estabeleceu o que **não** foi (rename), não o que foi. Descobrir exige olhar o estado real do banco de homologação — é a **primeira tarefa** do discovery, antes de qualquer correção.
  - **Correção provável, a confirmar:** proteger a categoria default contra exclusão e contra virar filha de outra, **ou** guardar uma referência estável (`Project.defaultCategoryId`, no padrão do `publishedVersionId` do item 24) em vez de resolver por slug + `parentId: null`. Hoje `defaultCategoryId` tem zero ocorrências no repositório.
- `[ ]` ⚠️ **49. Remix Icon como fonte de ícones do projeto, e a correção dos ícones que mentem.** _(pedido do humano, 2026-08-18; registrado a partir de conversa — **sem discovery**)_ Duas frentes de naturezas diferentes. **(a) Infraestrutura:** adicionar a [Remix Icon](https://remixicon.com/) como fonte de ícones disponível para uso geral, ao lado dos `MaterialIcons` — registro da fonte, tokens de acesso e a decisão de onde ela mora. **(b) Correção de UX:** os ícones do Inspector **não acompanham o eixo do widget selecionado** — com uma `column` selecionada, os controles de alinhamento e espaçamento exibem glifos de orientação errada (verticais onde deveriam ser horizontais, e vice-versa numa `row`). O ícone comunica o eixo errado, e o usuário lê o ícone antes do rótulo.
  - **A pergunta que define o escopo, e o discovery abre por ela:** a Remix Icon fica disponível só para o **chrome do editor**, ou também para o **spec SDUI** (o app cliente escolhendo ícone por nome)? Na segunda hipótese mexe em `packages/sdui_core/lib/src/catalog/widget_catalog.dart` e no `parsing/material_icons.dart` do renderer — e aí não é uma fase, são várias, com fixture nova e compatibilidade de specs já publicados.
  - **Risco técnico que o plano tem de nomear:** o repo já tem cicatriz de fonte de ícone — `apps/driva_editor/web/flutter_bootstrap.js` força `canvasKitVariant: "full"` porque a variante `chromium` falhava ao registrar o OTF de `MaterialIcons` e produzia "tofu", e o `Dockerfile` do editor builda com `--no-tree-shake-icons`. Uma segunda fonte entra exatamente nesse mecanismo. Pior: se a escolha do ícone passar a vir de string (nome no spec ou em config), aparece `IconData` construído dinamicamente — hoje **não existe nenhum** no app, no renderer nem no kernel, e é isso que mantém o tree-shaking viável. **O aceite desta fase inclui build release, não só debug.**
  - **Colisão com dois itens já registrados:** o **48** (tema e design tokens em cascata) — ícone é token, e onde a fonte se registra é decisão do `core/theme/`; e o **Gate 4** do `gates_guard.sh`, que exige token em vez de literal. ⚠️ **Decisão pendente do humano:** o 49 entra antes ou depois do 48? Antes significa refazer a fiação quando a cascata chegar.
  - **Licença da Remix Icon:** item de checagem do discovery, não de memória.
- `[ ]` **45. Topo do shell e rodapé do editor somem da árvore de acessibilidade.** _(achado pelo E2E de UI do item 24, 2026-08-16; 0-dep, mas investigação primeiro)_ Nenhum nó `flt-semantics` aparece acima de y≈86 nem na faixa do rodapé do editor — a barra de topo inteira (Salvar/Publish/⋮) e o rodapé de status (incluindo o `Semantics(liveRegion)` que carrega "Status: No ar (vN)" e os avisos de falha) **não existem** para leitor de tela, mesmo tendo `Semantics`/tooltip no código. Hipótese registrada pelo QA: o `BlockSemantics` da barreira de rota (go_router) derruba a casca pintada por cima antes dela. ⚠️ **Precisão da auditoria (2026-08-19):** `BlockSemantics` tem **zero ocorrências** em `apps/` e `packages/` — se ele está em jogo, vem do **framework**, não de código deste repositório. A hipótese continua plausível, mas a investigação começa no go_router/Flutter, não procurando a classe no projeto. **Vale para toda rota do editor**, não é regressão do item 24 — só foi descoberto porque o E2E daquele item precisou ler o estado de publicação por semântica e não achou nada, tendo que recorrer a varredura de clique + fingerprint de pixel como contorno. Quebra a promessa de acessibilidade de qualquer feature que dependa da barra de topo/rodapé (cor nunca pode ser o único sinal — regra do CLAUDE.md — mas hoje nem chega a sinal nenhum para quem usa leitor de tela).
- `[ ]` **40. O `backend/` ganha bateria de testes de verdade.** _(surgiu da F2 do item 39, 2026-08-15)_ Até o proxy de mídia, **o backend não tinha infraestrutura de teste nenhuma** — `jest`, `ts-jest`, `supertest` e `@nestjs/testing` nasceram ali de carona, junto de uma feature que precisava provar 11 controles de segurança. Dois sintomas disso já foram corrigidos na própria fase (o `jest-e2e.json` reintroduzia `commonjs`/`node`, o par que o time migrou de propósito para `nodenext`; e a suíte redeclarava o bootstrap em vez de reusar o `main.ts`, então a asserção de CORS testava uma cópia) — mas o buraco de fundo continua. **Corrigido em 2026-08-19 pela auditoria:** a CI **já** roda `pnpm lint` + `pnpm build` + `pnpm test` + `pnpm test:e2e` (`.github/workflows/ci.yml:153-196`) — a frase "em vez de só `pnpm build`" descrevia o passado. **O que resta é cobertura:** hoje só `media` (2 arquivos) e um recorte de `public` (o teste de rate limit da fatia 2 do item 25) têm testes. **Zero** para `contents`, `projects`, `categories` e `storage` — que é onde mora o CRUD inteiro do editor. **Enquanto não for item, isso continua nascendo de carona na próxima feature que precisar** — que é exatamente como chegou aqui.

---

## Débitos e riscos vivos (não são itens, são vigilâncias)

| Assunto | Estado | Onde está registrado |
| --- | --- | --- |
| Auth por `x-project-id` | Aceito para hml/demo; vira o item **26** antes de produção real | `docs/09-crud-projeto/variance_report.md` |
| Storage S3 não ligado | Local em hml; vira o item **27** | idem |
| E2E precisa exercitar a UI real no hml | Corrigido no item 9g; **regra permanente** para toda feature | memória `e2e-precisa-exercitar-de-verdade` |
| Bateria automatizada vem por último | Regra de método (cap. 22 do livro), não débito | `CLAUDE.md` › Método de trabalho |
| ~~API pública serve **rascunho**, não versão publicada~~ | **Fechado pelo item 24** (2026-08-17): a rota pública passou a servir `publishedVersionId` | `docs/24-publicacao-versionamento/final_report.md` |
| **Widget novo no spec derruba a página inteira em app antigo** | ⚠️ **Achado em 2026-08-19, sem item ainda.** `parseNode` devolve `Left` para `tipo "x" fora do catálogo` (`packages/sdui_core/lib/src/schema/node_schema.dart:30`), e como o parse é recursivo a partir da raiz **um nó desconhecido reprova o `ContentSpec` todo**. O app na loja tem o catálogo que foi compilado nele: publicar uma tela com um primitivo novo (item 9 é um track **contínuo**) faz o app antigo cair no fallback — a página inteira, não o widget. Pior, o renderer **já tem** a degradação graciosa por nó (a caixa `Tipo desconhecido: "<type>"`, `renderer.dart:101`), e ela é **inalcançável** nesse caminho: o parse falha antes. Hoje não dói porque nenhum app real consome; a fatia 2 do item 25 muda isso | verificado em código; guia de integração do item 25 › §7 |
| `latestVersion` da decisão A3 nunca foi implementado | Débito do item 24 achado em 2026-08-19; "fora do ar" aparece como "nunca publicado" em três telas. Vira a frente (c) do item **50** | `docs/14-publicacao-versionamento/prd.md:593-609` |
| Chave publicável sem rotação | Rate limit **fechado** na fatia 2 do item 25 (2026-08-19, 120 req/min). A **rotação** continua aberta e vai com o item 26 — `POST /v1/projects/:id/rotate-key` foi desenhado na D1 do plano do 25 e nunca implementado | `docs/13-loop-sdui/variance_report.md` VR-13-03/04 |
