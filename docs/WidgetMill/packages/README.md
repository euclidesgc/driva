# packages/

Pacotes TypeScript reutilizáveis do workspace pnpm, sob o escopo `@widgetmill/*`.

- **spec/** — `@widgetmill/spec`: o kernel. Schemas Zod do spec JSON (fonte de verdade), tipos derivados, fixtures golden. Não depende de nada interno.
