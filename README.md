# Driva

Server Driven UI para aplicações Flutter (BMJTech).
Inspirado no FlutterFlow (parte web) e no package `stack` (renderização de widgets no Flutter).

> Repositório do projeto Driva no Paperclip. Mini-site de documentação em `docs/` (ver runbook, Seção 3).
