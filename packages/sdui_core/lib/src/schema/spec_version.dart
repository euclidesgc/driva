/// Versão atual do formato do spec. Gancho para migração futura: quando o
/// formato mudar, `parsePageSpec` passa a migrar versões antigas antes de
/// validar (spec §versionamento).
const int kSpecVersion = 1;
