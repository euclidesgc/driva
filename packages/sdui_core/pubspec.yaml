name: sdui_core
description: Kernel do spec SDUI do driva — modelos, schema de validação e catálogo de widgets (Dart puro).
version: 0.2.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  equatable: ^2.0.7
  fpdart: ^1.1.1
  zard: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
